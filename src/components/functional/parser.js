export default function parser(type, url, schema) {
    // This function will parse an incoming RSS or JSON feed and return a standardised format

    if (type === 'rss') {
        // This will accept a RSS feed (predefined schema) and return a JSON array of the feed
        
    }
}